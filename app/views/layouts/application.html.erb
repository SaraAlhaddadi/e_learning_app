<!DOCTYPE html>
<html>
  <head>
    <title>ELearningApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-blue-500 py-4">
      <div class="container mx-auto flex justify-between items-center">
        <div>
          <%= link_to "E-learning website", root_path, class: "text-white font-semibold text-lg" %>
        </div>
        <div>
          <%= link_to "Courses", courses_path, class: "text-white hover:text-blue-200 ml-4" %>
          <%= link_to "Users", users_path, class: "text-white hover:text-blue-200 ml-4" %>
        </div>
      </div>
    </nav>

    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
